import { expect } from 'chai';

describe('App', () => {
  // placeholder so the whole thing doesn't fail since there are no tests
  it('will do it', () => {
    expect(true).to.be.true; // eslint-disable-line chai-expect/no-inner-literal
  });
});
